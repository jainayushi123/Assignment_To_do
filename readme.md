# Setup

```
python -m venv .venv

.venv\Scripts\Activate

pip install -r requirements.txt

created two environment files
.env.dev
.env.prod

(i will provid the env files in internshala chat for your reference)
```
